<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head")%>

    <%- include("../partials/header")%>
        <main class="producto-seccion">
            <h1>Usted esta por Eliminar este Producto: </h1>
                <img src="<%= product.photo %>" alt="<%= product.name %>">
                <div>
                    <h2>Stock: <%= product.stock %></h2>
                    <h2>SKU: <%= product.id %></h2>
                </div>
                <div class="administrar-producto">
                    <h1><%= product.name %></h1>
                    <a href="/<%= product.id %>/edit"><button>Modificar</button></a>
                    <form action="/delete/<%= product.id %>?_method=PUT" method="post">
                        <button type="submit">Eliminar</button>
                    </form>
                </div>
        </main>

        <%- include("../partials/footer")%>

</html>